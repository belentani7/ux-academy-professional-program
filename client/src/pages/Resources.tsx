import { PageHeading, Tag } from "@/components/AcademyUI";
import { useLocale } from "@/contexts/LocaleContext";
import { text } from "@/lib/i18n";
import { resourceLibrary } from "@shared/courseContent";
import { ArrowUpRight, ExternalLink } from "lucide-react";
import { useMemo, useState } from "react";
import { Link } from "wouter";

export default function Resources() { const { locale } = useLocale(); const [category, setCategory] = useState("all"); const categories = useMemo(() => Array.from(new Set(resourceLibrary.map(item => text(item.category, locale)))), [locale]); const visible = category === "all" ? resourceLibrary : resourceLibrary.filter(item => text(item.category, locale) === category); const googleCopy = locale === "es" ? { eyebrow: "Ampliación", title: "Ecosistema Google, con atribución.", detail: "16 rutas originales y trilingües conectan herramientas, decisiones, prácticas y fuentes verificadas.", action: "Explorar rutas" } : locale === "pt" ? { eyebrow: "Ampliação", title: "Ecossistema Google, com atribuição.", detail: "16 trilhas originais e trilíngues conectam ferramentas, decisões, práticas e fontes verificadas.", action: "Explorar trilhas" } : { eyebrow: "Expansion", title: "Google ecosystem, with attribution.", detail: "16 original trilingual tracks connect tools, decisions, practice, and verified sources.", action: "Explore tracks" }; return <><PageHeading eyebrow={locale === "es" ? "Biblioteca" : locale === "pt" ? "Biblioteca" : "Library"} title={locale === "es" ? "Fuentes para profundizar." : locale === "pt" ? "Fontes para aprofundar." : "Sources to go deeper."} description={locale === "es" ? "Documentación oficial, educación especializada y marcos complementarios verificados durante la curaduría curricular." : locale === "pt" ? "Documentação oficial, educação especializada e marcos complementares verificados durante a curadoria curricular." : "Official documentation, specialist education, and complementary frameworks verified during curriculum curation."} /><section className="mb-9 rounded-3xl bg-[#1d382d] p-6 text-[#f7f4ee] md:flex md:items-end md:justify-between md:gap-8"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#d5ae7b]">{googleCopy.eyebrow}</p><h2 className="mt-3 font-serif text-3xl leading-tight md:text-4xl">{googleCopy.title}</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#d4ded3]">{googleCopy.detail}</p></div><Link href="/google-ecosystem" className="mt-5 inline-flex shrink-0 items-center gap-2 rounded-full bg-[#d7a15f] px-5 py-3 text-sm font-bold text-[#1d382d] transition hover:bg-[#e4b97f] md:mt-0">{googleCopy.action}<ArrowUpRight size={16}/></Link></section><div className="mb-7 flex flex-wrap gap-2"><button onClick={() => setCategory("all")} className={`rounded-full px-3 py-1.5 text-xs font-bold ${category === "all" ? "bg-[#1d382d] text-white" : "bg-[#e9e3d9] text-[#5c665c]"}`}>{locale === "es" ? "Todo" : locale === "pt" ? "Todos" : "All"}</button>{categories.map(item => <button key={item} onClick={() => setCategory(item)} className={`rounded-full px-3 py-1.5 text-xs font-bold ${category === item ? "bg-[#1d382d] text-white" : "bg-[#e9e3d9] text-[#5c665c]"}`}>{item}</button>)}</div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{visible.map(item => <a key={item.url} href={item.url} target="_blank" rel="noreferrer" className="group rounded-2xl border border-[#dfd8ce] bg-[#fffdf9] p-5 transition hover:-translate-y-0.5 hover:border-[#b98a58]"><div className="flex items-start justify-between gap-2"><Tag tone={text(item.type, locale) === (locale === "es" ? "Oficial" : locale === "pt" ? "Oficial" : "Official") ? "forest" : "neutral"}>{text(item.type, locale)}</Tag><ExternalLink size={16} className="text-[#7d867c]"/></div><h2 className="mt-5 font-serif text-2xl leading-tight">{item.title}</h2><p className="mt-2 text-sm text-[#657064]">{text(item.category, locale)}</p><span className="mt-6 inline-flex items-center gap-1 text-sm font-bold text-[#2f6048]">{locale === "es" ? "Abrir fuente" : locale === "pt" ? "Abrir fonte" : "Open source"}<ArrowUpRight size={15}/></span></a>)}</div></>; }
